[global]
# Pip configuration for {{ app_name }}
cache-dir = {{ pip_cache_dir }}
timeout = 60
trusted-host = pypi.org
               pypi.python.org
               files.pythonhosted.org

[install]
# Use system site packages if needed
# system-site-packages = false