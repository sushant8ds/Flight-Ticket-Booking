version: '3.8'

services:
  web:
    build: .
    command: ["/app/entrypoint.sh", "gunicorn", "capstone.wsgi:application", "--bind", "0.0.0.0:8000", "--workers", "3"]
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped
